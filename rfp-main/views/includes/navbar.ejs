<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">FWMS</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="navbar-nav me-auto mb-2 mb-lg-0">
        <a class="nav-link active" aria-current="page" href="/">Home</a>
        <a class="nav-link" aria-current="page" href="/departments"
          >departments</a
        >
        <% if (currUserStatus && currUserStatus.role === 'admin') { %>
        <a class="nav-link" aria-current="page" href="/teachers">teachers</a>
        <% } %>
        <a class="nav-link" aria-current="page" href="/findfaculty"
          >findfaculty</a
        >
        <% if (currUserStatus) { %> <% if (currUserStatus.role === 'admin') { %>
        <a class="nav-link" aria-current="page" href="/subjects">subjects</a>
        <% } %> <% if (currUserStatus.role === 'teacher') { %>
        <a class="nav-link" aria-current="page" href="/deptannouncements"
          >announcements</a
        >
        <% } %> <% } %>
      </div>
      <div class="ms-auto navbar-nav">
        <% if (!currUserStatus) { %>
        <a class="nav-link" href="/login">Login</a>
        <a class="nav-link" href="/signup">Sign Up</a>
        <% } else { %> <% if (currUserStatus.role === 'teacher') { %>
        <a class="nav-link" aria-current="page" href="/profile">Profile</a>
        <% } %>
        <a class="nav-link" href="/logout">Logout</a>
        <% } %>
      </div>
    </div>
      
  </div>
</nav>
